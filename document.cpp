#include<stdio.h>
#include<stdbool.h> 
int main(){
	//variable =A reusable container for a value 
//	 behaves as if it were the value if contains 

//int =whole numbers (4 bytes in mordern systems)
//float = single-precision decimal number (4 bytes)
//double = double-precision decimal number (8 byte)
//char = single character (1 byte)
//char = array of caracter (size varies)
//bool = true or false (1 byte requireds <stdbool.h>)
return 0;
}
